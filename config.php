<?php
/**
 * Absolute path to theme's directory
 */
if ( ! defined( 'WPEMERGETHEME_DIR' ) ) {
	define( 'WPEMERGETHEME_DIR', __DIR__ );
}

/**
 * Absolite path to theme's src directory
 */
if ( ! defined( 'WPEMERGETHEME_SRC_DIR' ) ) {
	define( 'WPEMERGETHEME_SRC_DIR', WPEMERGETHEME_DIR . DIRECTORY_SEPARATOR . 'src' . DIRECTORY_SEPARATOR );
}

/**
 * Absolite path to theme's src directory
 */
if ( ! defined( 'WPEMERGETHEME_HELPERS_DIR' ) ) {
	define( 'WPEMERGETHEME_HELPERS_DIR', WPEMERGETHEME_DIR . DIRECTORY_SEPARATOR . 'helpers' . DIRECTORY_SEPARATOR );
}
